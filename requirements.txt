# This file is kept for legacy compatibility
# For modern dependency management, use:
#   uv sync                    # Install all dependencies
#   uv sync --extra dev        # Install with development dependencies
#   uv sync --extra test       # Install with test dependencies
#   uv sync --extra ldap       # Install with LDAP support
#
# All dependencies are defined in pyproject.toml

# Install the project in editable mode -e .

# Additional dependencies installed for full functionality:
django-flags>=5.0.0
python-ldap>=3.4.0
django-auth-ldap>=5.2.0
ldap-filter>=1.0.1
